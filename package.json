{
  "name": "notes",
  "version": "1.0.0",
  "description": "An simple app to management of notes ",
  "main": "js/app.js",
  "scripts": {
    "public": "mkdir -p public/js",
    "files": "npm run public && cp -r src/css public && cp src/index.html public", 
    "build": "browserify -t babelify ./src/react/app.js -o ./public/js/app.js",
    "server": "node server",
    "start": "npm run files && npm run build && npm run server"
  },
  "author": "@luixlacrux > Luis Lacruz",
  "license": "MIT",
  "dependencies": {
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.11.1",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "express": "^4.14.0",
    "react": "^15.2.1",
    "react-dom": "^15.2.1"
  }
}
